<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amitrawat.loginform">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SubActivity"
            android:label="Hello User"
            />
        <activity
            android:name=".AltnewsActivity"
            android:label="Alt News"
            />
        <activity
            android:name=".IndiatodayActivity"
            android:label="India Today"
            />
        <activity
            android:name=".NdtvActivity"
            android:label="NDTV"
            />
        <activity
            android:name=".AbpActivity"
            android:label="ABP News"
            />
        <activity
            android:name=".FirstpostActivity"
            android:label="FP"
            />
        <activity
            android:name=".ToiActivity"
            android:label="TOI" >

        </activity>

    </application>

</manifest>